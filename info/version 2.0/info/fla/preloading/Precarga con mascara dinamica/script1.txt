mascara._yscale = 0;
this.onEnterFrame = function() {
	porcentage = int((getBytesLoaded()/getBytesTotal())*100);
	if (porcentage == 100) {
		delete this.onEnterFrame;
		// aqui la peli ya está cargada, escribe tu código (gotoAnd....) etc.
		gotoAndStop(2);
	} else {
		mascara._yscale = porcentage;
	}
};